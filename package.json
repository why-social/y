{
  "name": "backend-frontend",
  "version": "0.1.0",
  "description": "Y is a Twitter (X) clone built for Web and Mobile Engineering (DIT342)",
  "main": "server/app.js",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "prod": "npm run build --prefix client && npm run start --prefix server"
  },
  "engines": {
    "npm": "9.x",
    "node": "18.x"
  }
}